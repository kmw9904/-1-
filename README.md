# javascript-calculator-precourse

## 구현할 기능 목록 정리

1. 입출력 기능  
   1.1. `@woowacourse/mission-utils`에서 제공하는 `Console.readLineAsync()`를 활용하여 입력 기능 구현과 `Console.print()`로 입력한 값이 제대로 출력할 수 있는 기능 구현

2. 구분자와 양수로 구성된 입력값을 구분자로 구분하는 로직 기능 구현  
   2.1. 쉼표(,) 또는 콜론(:)을 구분자로 숫자를 분리하는 기능 구현  
   2.2. "//" 와 "\n" 사이에 위치하는 커스텀 구분자를 구현하는 기능 구현  
   2.3. 숫자의 범위가 정해진게 없으니 음수 부호의 숫자 예외처리 기능 구현

3. 구분된 숫자를 가지고 더한 값을 출력 기능  
   3.1. 숫자를 더하고 출력하는 기능 구현  
   3.2. 사용자가 잘못된 값을 입력할 경우 `[Error]`로 시작하는 메시지 출력 기능 구현  
   3.3. `[Error]`가 출력될 경우 Error가 발생 시킨 후 애플리케이션 종료 기능 구현

## 기능 구현 전 어렵게 생각했던 것

1. 실행 결과 예시에 문구를 어떻게 만들어가는 걸까?
2. 기존 prompt 메서드와 console.log()의 출력메서드만 알고 있는 상태에서 새로운 라이브러리의 입력 및 출력을 어떻게 할지 감이 안옴

## 구현하면서 바뀐 기능

2. 구분자 처리 순서 변경
   - 먼저 입력 시작이 숫자인지, // 문자열인지 판별
   - // 일 경우 -> CustomDelimiter 기능으로 구분자 추출
   - 추출된 구분자로 입력값이 올바른 구분자로 구성됐는지 검증
   - 마지막으로 숫자를 합산하는 Parse 기능 구현
     ...

## 기능 구현 후 배운점

1. 문제를 열심히 읽자... 문제를 제대로 안읽어 음수를 구하느라 시간이 많이 씀...
2. 이번에 새로운 입력 방법을 배웠다. 맨날 require('fs').readFileSync 방식으로 입력을 받았지만 새로운 방식으로 입력을 받는 방법과 그 과중 replace를 통해 \n을 문자열로 받아오는 법이 제일 보람차고 알찼다
3. 예외처리를 항상 생각하게 되었다. 항상 "예외 케이스가 어떤 것이 있을까?"를 생각하면서 코드를 다시 만들고 하느라 시간이 오래걸린것 같았지만, 기초를 더욱 탄탄하게 다지는 방법을 배운것 같다.
4. 문제를 접했을 때 어떻게 순서대로 접근할지 방법이 생긴거 같다.
   ...
